# import pandas as pd

# df = pd.DataFrame([[1, 2, 3], [2, 5, 7], [7, 8, 9]], columns=['A', 'B', 'C'])

# print(len(df))
# df.loc[len(df)] = [1, 2, 3]  # list you want to insert
# # df.index = df.index + 1  # shifting index
# # df = df.sort_index()  # sorting by index
# print(df)
print([2]*3)
